{
  "name": "ecommerce-shootout",
  "private": true,
  "version": "0.1.0",
  "description": "Root monorepo dev orchestrator & Nx dependencies for the 1 frontend + 11 backend polyglot e-commerce shootout",
  "scripts": {
    "build:affected": "nx affected -t build",
    "containers:affected": "nx affected -t container-build",
    "bench:analyze": "nx run shared-benchmarks:analyze",
    "compose:up": "docker compose up -d",
    "compose:down": "docker compose down",
    "proto:generate": "buf generate"
  },
  "devDependencies": {
    "nx": "20.2.2"
  }
}
