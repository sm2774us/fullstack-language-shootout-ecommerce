apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-golang
  namespace: ecommerce-shootout
  labels: { app: backend-golang }
spec:
  replicas: 2
  selector:
    matchLabels: { app: backend-golang }
  template:
    metadata:
      labels: { app: backend-golang }
    spec:
      containers:
        - name: backend-golang
          image: backend-golang:latest
          ports: [{ containerPort: 8080 }]
          readinessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 5
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: backend-golang
  namespace: ecommerce-shootout
spec:
  selector: { app: backend-golang }
  ports: [{ port: 8080, targetPort: 8080 }]
