apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-cpp
  namespace: ecommerce-shootout
  labels: { app: backend-cpp }
spec:
  replicas: 2
  selector:
    matchLabels: { app: backend-cpp }
  template:
    metadata:
      labels: { app: backend-cpp }
    spec:
      containers:
        - name: backend-cpp
          image: backend-cpp:latest
          ports: [{ containerPort: 8080 }]
          readinessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 5
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: backend-cpp
  namespace: ecommerce-shootout
spec:
  selector: { app: backend-cpp }
  ports: [{ port: 8080, targetPort: 8080 }]
