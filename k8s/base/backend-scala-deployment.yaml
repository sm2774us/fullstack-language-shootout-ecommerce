apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-scala
  namespace: ecommerce-shootout
  labels: { app: backend-scala }
spec:
  replicas: 2
  selector:
    matchLabels: { app: backend-scala }
  template:
    metadata:
      labels: { app: backend-scala }
    spec:
      containers:
        - name: backend-scala
          image: backend-scala:latest
          ports: [{ containerPort: 8080 }]
          readinessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 5
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: backend-scala
  namespace: ecommerce-shootout
spec:
  selector: { app: backend-scala }
  ports: [{ port: 8080, targetPort: 8080 }]
