apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-r
  namespace: ecommerce-shootout
  labels: { app: backend-r }
spec:
  replicas: 2
  selector:
    matchLabels: { app: backend-r }
  template:
    metadata:
      labels: { app: backend-r }
    spec:
      containers:
        - name: backend-r
          image: backend-r:latest
          ports: [{ containerPort: 8080 }]
          readinessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 5
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: backend-r
  namespace: ecommerce-shootout
spec:
  selector: { app: backend-r }
  ports: [{ port: 8080, targetPort: 8080 }]
