apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-kotlin
  namespace: ecommerce-shootout
  labels: { app: backend-kotlin }
spec:
  replicas: 2
  selector:
    matchLabels: { app: backend-kotlin }
  template:
    metadata:
      labels: { app: backend-kotlin }
    spec:
      containers:
        - name: backend-kotlin
          image: backend-kotlin:latest
          ports: [{ containerPort: 8080 }]
          readinessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 5
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: backend-kotlin
  namespace: ecommerce-shootout
spec:
  selector: { app: backend-kotlin }
  ports: [{ port: 8080, targetPort: 8080 }]
