apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-ruby
  namespace: ecommerce-shootout
  labels: { app: backend-ruby }
spec:
  replicas: 2
  selector:
    matchLabels: { app: backend-ruby }
  template:
    metadata:
      labels: { app: backend-ruby }
    spec:
      containers:
        - name: backend-ruby
          image: backend-ruby:latest
          ports: [{ containerPort: 8080 }]
          readinessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 5
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: backend-ruby
  namespace: ecommerce-shootout
spec:
  selector: { app: backend-ruby }
  ports: [{ port: 8080, targetPort: 8080 }]
