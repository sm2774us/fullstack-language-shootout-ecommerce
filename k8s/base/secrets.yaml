# Placeholder Secret manifest. NEVER commit real values here — in practice
# this is generated via `kubectl create secret generic ... --from-literal`
# or, better, sourced from your cloud's secret manager (AWS Secrets
# Manager, Azure Key Vault, GCP Secret Manager) using each provider's CSI
# driver / External Secrets Operator so the raw value never touches git.
apiVersion: v1
kind: Secret
metadata:
  name: shootout-secrets
  namespace: ecommerce-shootout
type: Opaque
stringData:
  STRIPE_SECRET_KEY: "REPLACE_ME"
  STRIPE_WEBHOOK_SECRET: "REPLACE_ME"
  DATABASE_URL: "REPLACE_ME"
